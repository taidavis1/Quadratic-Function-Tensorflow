# Quadratic-Function-Tensorflow

#Main Idea:
  - Understand more with Tensorflow and Neural Network
  - To see how tensorflow would train the quadratic function.
  
# Install packages: 
  # 1) pip install matplotlib
  # 2) pip install tensorflow
  # 3) pip install scikit-learn
 
